[project]
name = "Gauss summation formula"
blueprint = "blueprint.md"

[isabelle]
session = "Gauss_Demo"
# executable = "isabelle"      # uncomment if `isabelle` is not on PATH
# dirs = ["."]                  # extra -d directories passed to `isabelle build`

[output]
build_dir = "build"
site_dir  = "site"
